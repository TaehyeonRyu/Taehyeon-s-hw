#include <iostream>
using namespace std;
const float PI = 3.141592;
void main() {
	float a, b, dt, t, num, anal;
	//differentiation
	cout << " a b ? = ";
	cin >> a >> b;
	cout << " dt t ? = ";
	cin >> t >> dt;
	t = t * PI / 180;
	anal = a * b * sec^2(b*t);


}